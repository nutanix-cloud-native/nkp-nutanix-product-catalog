schema: catalog.nkp.nutanix.com/v1/catalog-release-spec
# NOTE:
# - Keep <=2.19 entries on legacy "x-airgapped" tags and "airgapped-..." file names.
# - For versions >2.19, add entries using "<major>.<minor>.x-full"
#   and file names prefixed with "full-".
releases:
  - tagName: "2.18.x-airgapped"
    fileName: airgapped-nkp-nutanix-product-catalog-2.18.tar
    applications:
      - name: ndk
        version: "2.1.0"

  - tagName: "2.19.x-airgapped"
    fileName: airgapped-nkp-nutanix-product-catalog-2.19.tar
    applications:
      - name: ndk
        version: "2.1.0"
      # We can come back to this once nutanix-ai inclusion is finalised due to large bundle sizes.
