schema: catalog.nkp.nutanix.com/v1/application-metadata
displayName: Nutanix Data Hub (NDH)
description: Nutanix Data Hub is a comprehensive data platform for Kubernetes.
type: custom
allowMultipleInstances: false
category:
- data-platform
- data-services
dependencies:
  - kube-prometheus-stack
  - loki
icon: "" # TODO: Sanath, please provide the icon
licensing:
- Pro
- Ultimate
overview: |
  # Nutanix Data Hub (NDH)

  Nutanix Data Hub (NDH) is a comprehensive data platform for Kubernetes.

  ## Prerequisites (REQUIRED)

  Before enabling NDH, create the following secrets on the target cluster.

  ### Container Image Registry Secrets (Artifactory)

  Create in both application namespaces:

  ```bash
  # For NDH core components
  kubectl create namespace ndh-core
  kubectl create secret docker-registry ndh-image-pull-secret \
    --namespace=ndh-core \
    --docker-server=<your-artifactory-server> \
    --docker-username=<your-username> \
    --docker-password=<your-api-key>

  # For NDH operators
  kubectl create namespace ndh-operators
  kubectl create secret docker-registry ndh-image-pull-secret \
    --namespace=ndh-operators \
    --docker-server=<your-artifactory-server> \
    --docker-username=<your-username> \
    --docker-password=<your-api-key>
  ```

  ## Resource Requirements

  NDH requires approximately 8 worker nodes with 4 CPU / 8GB RAM each.
scope:
- workspace
supportLink: "https://google.com"
---
