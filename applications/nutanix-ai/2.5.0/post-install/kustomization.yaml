apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - job.yaml
  - node-exporter-rbac-cluster-role.yaml
  # Resources applier job (Certificate + ServiceMonitor + ConfigMap)
  - apply-resources-job.yaml