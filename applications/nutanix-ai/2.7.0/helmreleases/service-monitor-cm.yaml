---
apiVersion: v1
kind: ConfigMap
metadata:
  name: service-monitor-defaults
  namespace: ${workspaceNamespace}
data:
  values.yaml: |-
    naiMonitoring:
      nodeExporter:
        serviceMonitor:
          namespaceSelector:
            matchNames:
            - ${workspaceNamespace}
      dcgmExporter:
        serviceMonitor:
          namespaceSelector:
            matchNames:
            - ${workspaceNamespace}
